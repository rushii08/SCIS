@model SCIS.ViewModels.ClubViewModel

@{
    ViewData["Title"] = "Delete Club";
}

<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">Delete Club</h1>
    <a asp-action="Index" class="d-none d-sm-inline-block btn btn-secondary shadow-sm">
        <i class="fas fa-arrow-left fa-sm text-white-50 me-2"></i>Back to List
    </a>
</div>

<div class="card shadow mb-4 border-left-danger">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-danger">Warning</h6>
    </div>
    <div class="card-body">
        <div class="alert alert-danger">
            <i class="fas fa-exclamation-triangle me-2"></i>
            <strong>Are you sure you want to delete this club?</strong> This action cannot be undone.
            <br>
            <small>All memberships, events, and announcements associated with this club will also be deleted.</small>
        </div>
        
        <div class="mb-4">
            <h5 class="font-weight-bold">Club Details</h5>
            <dl class="row">
                <dt class="col-sm-3">Name</dt>
                <dd class="col-sm-9">@Model.Name</dd>
                
                <dt class="col-sm-3">Description</dt>
                <dd class="col-sm-9">@Model.Description</dd>
                
                <dt class="col-sm-3">President</dt>
                <dd class="col-sm-9">@Model.PresidentName</dd>
                
                <dt class="col-sm-3">Created By</dt>
                <dd class="col-sm-9">@Model.CreatedByAdminName</dd>
                
                <dt class="col-sm-3">Created At</dt>
                <dd class="col-sm-9">@Model.CreatedAt.ToString("MMMM dd, yyyy")</dd>
            </dl>
        </div>
        
        <form asp-action="Delete" method="post">
            <input type="hidden" asp-for="ClubId" />
            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                <a asp-action="Index" class="btn btn-secondary me-md-2">Cancel</a>
                <button type="submit" class="btn btn-danger">
                    <i class="fas fa-trash me-1"></i> Delete Club
                </button>
            </div>
        </form>
    </div>
</div>
